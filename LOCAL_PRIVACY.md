# 本地私有版

本分支保留网页翻译、划词翻译、自定义 AI 动作、已有字幕翻译、翻译工作台和本地配置备份。

已移除登录、会员、官方托管 AI、Google Drive 同步、云端笔记、产品导航、最近更新、统计上报、在线视频转录和文本转语音。图标随插件打包，不从 CDN 加载；AI 输出中的图片也不会自动联网加载。

设置、提供商密钥、翻译缓存和自动备份保存在当前浏览器的本地存储或 IndexedDB 中。默认提供 Google 和 Microsoft 在线基础翻译，无需登录或 API Key；不会在启动时探测翻译服务或自动翻译网站。也可以在设置中添加自定义 Chat Completions 或 Responses 服务，填写在线或本机服务地址和模型，再为所需功能选择该服务。原有自行配置的提供商仍可使用。

调用翻译、摘要或 AI 动作时，相关文本会发送给你选择的服务。如果启用智能上下文，也会发送相关网页上下文。要让文本完全不离开设备，请使用本机模型服务；填写云端服务地址意味着主动使用该服务。字幕功能按需读取当前视频网站的字幕。

升级会保留自定义提供商、提示词和其他本地设置，移除已删除功能的配置，并为基础翻译修复失效的服务引用，自定义 AI 功能在未配置时显示占位提示。历史迁移脚本保留原始快照以支持旧配置导入。备份文件只在用户主动导出时写入所选位置；含密钥导出会把密钥一并保存到文件。

开发与加载：

```powershell
pnpm install
pnpm run type-check
$env:SKIP_FREE_API = 'true'
pnpm test
pnpm build
```

在浏览器扩展管理页启用开发者模式，加载 `.output/chrome-mv3`。Firefox 构建使用 `pnpm build:firefox`。
